{
  "version": 2,
  "builds": [
    {
      "src": "linkedin-frontend/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "routes": [
    {
      "src": "^/(.*)",
      "dest": "/linkedin-frontend/dist/$1"
    }
  ]
}
